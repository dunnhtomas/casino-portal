---
import SimplePageLayout from '../../components/Layout/SimplePageLayout.astro';

// Generate static paths for all casino reviews
export async function getStaticPaths() {
  // Use import.meta.glob to load JSON at build time
  const dataFiles = import.meta.glob('../../../data/*.json', { eager: true });
  const casinosFile = dataFiles['../../../data/casinos.json'] as any;
  const casinosData = casinosFile?.default || [];
  
  return casinosData.map((casino: any) => ({
    params: { slug: casino.slug },
    props: { casino }
  }));
}

interface Casino {
  brand: string;
  slug: string;
  url?: string;
  affiliate?: { link: string };
  ratings?: Record<string, number>;
  bonuses?: { welcome?: { headline: string; wagering: string } };
  providers?: string[];
  banking?: string[];
  licences?: string[];
  [key: string]: any;
}

interface Props {
  casino: Casino;
}

const { casino } = Astro.props as Props;
const { slug } = Astro.params;

// Generate page metadata
const pageTitle = `${casino.brand} Review - Expert Analysis & Ratings`;
const pageDescription = `Comprehensive ${casino.brand} review with detailed analysis of games, bonuses, security, and customer service.`;

// Generate rating stars
function generateStars(rating: number) {
  return Array(5).fill(0).map((_, i) => i < Math.floor(rating / 2) ? '★' : '☆').join('');
}

// Calculate overall rating
const overallRating = casino.ratings ? 
  (Object.values(casino.ratings) as number[]).reduce((sum: number, val: number) => sum + val, 0) / Object.keys(casino.ratings).length : 0;

// Generate breadcrumbs
const breadcrumbs = [
  { label: 'Home', href: '/' },
  { label: 'Reviews', href: '/reviews' },
  { label: casino.brand, href: '' }
];
---

<SimplePageLayout 
  title={pageTitle}
  description={pageDescription}
  breadcrumbs={breadcrumbs}
>
  <main class="min-h-screen bg-gradient-to-br from-gray-50 to-gray-100">
    <article class="container mx-auto px-6 py-8 max-w-7xl">
      <!-- Hero Section -->
      <div class="review-hero">
        <div class="review-brand-info">
          <div class="flex items-start space-x-6">
            <div class="flex-shrink-0">
              <img 
                src={casino.logo?.url || '/images/casinos/default-casino.svg'} 
                alt={`${casino.brand} logo`} 
                class="w-20 h-20 object-contain rounded-2xl shadow-lg bg-gray-800 p-2 border border-gray-700"
              />
            </div>
            <div class="flex-grow">
              <h1 class="review-brand-title">{casino.brand}</h1>
              <p class="review-brand-subtitle mb-4">Comprehensive Expert Review & Analysis</p>
              <div class="flex flex-wrap gap-2">
                {casino.topTags?.map((tag: string) => (
                  <span class="bg-blue-100 text-blue-800 px-3 py-1 rounded-full text-sm font-medium">
                    {tag}
                  </span>
                ))}
              </div>
            </div>
          </div>
          <div class="review-rating-section flex-shrink-0">
            <div class="bg-gradient-to-r from-amber-500 to-orange-500 text-white px-8 py-4 rounded-2xl font-bold text-3xl inline-block shadow-lg">
              <div class="flex items-center space-x-2">
                <svg class="w-8 h-8" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path>
                </svg>
                <span>{overallRating ? overallRating.toFixed(1) : 'N/A'}</span>
              </div>
            </div>
            <p class="text-sm text-gray-600 mt-2 font-medium">
              {overallRating > 8 ? 'Excellent' : overallRating > 6 ? 'Good' : 'Fair'} Rating
            </p>
            <p class="text-xs text-gray-500 mt-1">Overall Rating</p>
          </div>
        </div>
      </div>

      <!-- Quick Facts Grid -->
      <section class="review-content-section">
        <h2 class="review-section-title">Quick Facts & Overview</h2>
        <div class="review-quick-facts">
          <div class="review-quick-fact-item bg-gradient-to-br from-gray-800 to-gray-700 border border-blue-500/30 relative overflow-hidden">
            <div class="absolute top-0 right-0 w-20 h-20 bg-gray-600/20 rounded-full -mr-10 -mt-10"></div>
            <div class="relative">
              <div class="w-12 h-12 bg-gradient-to-r from-blue-500 to-blue-600 rounded-xl flex items-center justify-center text-white text-xl mb-4 mx-auto shadow-lg">
                🛡️
              </div>
              <h3 class="font-bold text-white mb-6 text-lg">🛡️ Safety & Security</h3>
              <div class="space-y-4">
                <div class="bg-gray-900/60 rounded-lg p-3 border border-gray-600/40">
                  <div class="flex justify-between items-center">
                    <span class="text-sm text-gray-300 font-medium">License</span>
                    <span class="text-sm font-bold text-white bg-gray-700 px-2 py-1 rounded-md">
                      {casino.licences?.join(', ') || 'Not specified'}
                    </span>
                  </div>
                </div>
                <div class="bg-gray-900/60 rounded-lg p-3 border border-gray-600/40">
                  <div class="flex justify-between items-center">
                    <span class="text-sm text-gray-300 font-medium">Years Online</span>
                    <span class="text-sm font-bold text-white bg-gray-700 px-2 py-1 rounded-md">
                      {casino.safety?.yearsOnline || '5+'}
                    </span>
                  </div>
                </div>
                <div class="bg-gray-900/60 rounded-lg p-3 border border-gray-600/40">
                  <div class="flex justify-between items-center">
                    <span class="text-sm text-gray-300 font-medium">Security Score</span>
                    <span class="text-sm font-bold text-white bg-gray-700 px-2 py-1 rounded-md">
                      {casino.ratings?.security ? `${casino.ratings.security}/10` : '8.5/10'}
                    </span>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <div class="review-quick-fact-item bg-gradient-to-br from-gray-800 to-gray-700 border border-green-500/30 relative overflow-hidden">
            <div class="absolute top-0 right-0 w-20 h-20 bg-gray-600/20 rounded-full -mr-10 -mt-10"></div>
            <div class="relative">
              <div class="w-12 h-12 bg-gradient-to-r from-green-500 to-green-600 rounded-xl flex items-center justify-center text-white text-xl mb-4 mx-auto shadow-lg">
                💳
              </div>
              <h3 class="font-bold text-white mb-6 text-lg">💰 Banking & Payments</h3>
              <div class="space-y-4">
                <div class="bg-gray-900/60 rounded-lg p-3 border border-gray-600/40">
                  <div class="flex justify-between items-center">
                    <span class="text-sm text-gray-300 font-medium">Payment Methods</span>
                    <span class="text-sm font-bold text-white bg-gray-700 px-2 py-1 rounded-md">
                      {casino.banking?.length || 2}
                    </span>
                  </div>
                </div>
                <div class="bg-gray-900/60 rounded-lg p-3 border border-gray-600/40">
                  <div class="flex justify-between items-center">
                    <span class="text-sm text-gray-300 font-medium">Payout Speed</span>
                    <span class="text-sm font-bold text-white bg-gray-700 px-2 py-1 rounded-md">
                      {casino.payoutSpeedHours ? `${casino.payoutSpeedHours}h` : '24-72h'}
                    </span>
                  </div>
                </div>
                <div class="bg-gray-900/60 rounded-lg p-3 border border-gray-600/40">
                  <div class="flex justify-between items-center">
                    <span class="text-sm text-gray-300 font-medium">Payout Rating</span>
                    <span class="text-sm font-bold text-white bg-gray-700 px-2 py-1 rounded-md">
                      {casino.ratings?.payout ? `${casino.ratings.payout}/10` : '8/10'}
                    </span>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <div class="review-quick-fact-item bg-gradient-to-br from-gray-800 to-gray-700 border border-purple-500/30 relative overflow-hidden">
            <div class="absolute top-0 right-0 w-20 h-20 bg-gray-600/20 rounded-full -mr-10 -mt-10"></div>
            <div class="relative">
              <div class="w-12 h-12 bg-gradient-to-r from-purple-500 to-purple-600 rounded-xl flex items-center justify-center text-white text-xl mb-4 mx-auto shadow-lg">
                🎲
              </div>
              <h3 class="font-bold text-white mb-6 text-lg">🎮 Games & Software</h3>
              <div class="space-y-4">
                <div class="bg-gray-900/60 rounded-lg p-3 border border-gray-600/40">
                  <div class="flex justify-between items-center">
                    <span class="text-sm text-gray-300 font-medium">Game Providers</span>
                    <span class="text-sm font-bold text-white bg-gray-700 px-2 py-1 rounded-md">
                      {casino.providers?.length || 5}
                    </span>
                  </div>
                </div>
                <div class="bg-gray-900/60 rounded-lg p-3 border border-gray-600/40">
                  <div class="flex justify-between items-center">
                    <span class="text-sm text-gray-300 font-medium">Games Rating</span>
                    <span class="text-sm font-bold text-white bg-gray-700 px-2 py-1 rounded-md">
                      {casino.ratings?.games ? `${casino.ratings.games}/10` : '9/10'}
                    </span>
                  </div>
                </div>
                <div class="bg-gray-900/60 rounded-lg p-3 border border-gray-600/40">
                  <div class="flex justify-between items-center">
                    <span class="text-sm text-gray-300 font-medium">Live Dealer</span>
                    <span class="text-sm font-bold text-white bg-gray-700 px-2 py-1 rounded-md">
                      {casino.providers?.some((p: string) => p.toLowerCase().includes('evolution')) ? 'Yes' : 'Available'}
                    </span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Detailed Review Content -->
      <section class="review-content-section">
        <h2 class="review-section-title">Detailed Review Analysis</h2>
        <div class="bg-gray-800 rounded-2xl shadow-xl p-8 mb-8 border border-gray-700">
          <h2 class="text-2xl font-bold text-white mb-6">About {casino.brand}</h2>
          <div class="prose max-w-none">
            <p class="text-gray-300 leading-relaxed text-lg">
              Our expert review team has thoroughly analyzed {casino.brand} to provide you with comprehensive insights about this online casino. 
              Licensed under {casino.licences?.join(' and ') || 'reputable authorities'}, this casino offers a secure gaming environment 
              with {casino.providers?.length || 'multiple'} game providers and 
              {casino.banking?.length || 'various'} payment methods.
            </p>
            
            {casino.bonuses?.welcome && (
              <div class="mt-6 p-4 bg-amber-500/10 border border-amber-500/20 rounded-lg">
                <h3 class="text-xl font-bold text-amber-400 mb-2">Welcome Bonus</h3>
                <p class="text-gray-300">
                  New players can enjoy {casino.bonuses.welcome.headline} with {casino.bonuses.welcome.wagering} wagering requirements.
                </p>
              </div>
            )}
            
            {casino.providers && casino.providers.length > 0 && (
              <div class="mt-6">
                <h3 class="text-xl font-bold text-white mb-3">Game Providers</h3>
                <div class="flex flex-wrap gap-2">
                  {casino.providers.slice(0, 6).map((provider: string) => (
                    <span class="bg-gray-700 text-gray-200 px-3 py-1 rounded-md text-sm">
                      {provider}
                    </span>
                  ))}
                  {casino.providers.length > 6 && (
                    <span class="bg-gray-700 text-gray-400 px-3 py-1 rounded-md text-sm">
                      +{casino.providers.length - 6} more
                    </span>
                  )}
                </div>
              </div>
            )}
          </div>
        </div>
      </section>

      <!-- Rating Breakdown -->
      <section class="review-content-section">
        <h2 class="review-section-title">Rating Breakdown</h2>
        <div class="bg-gray-800 rounded-2xl shadow-xl p-8 mb-8 border border-gray-700">
          <div class="flex justify-between items-center mb-6">
            <h2 class="text-2xl font-bold text-white">Rating Breakdown</h2>
            <a href="/guides/how-we-rate" class="text-amber-600 hover:text-amber-700 text-sm font-medium">
              How We Rate →
            </a>
          </div>
          
          {casino.ratings && (
            <div class="grid md:grid-cols-2 gap-6">
              {Object.entries(casino.ratings).map(([key, value]: [string, any]) => (
                <div class="bg-gray-900/60 rounded-lg p-4 border border-gray-600/40">
                  <div class="flex justify-between items-center mb-2">
                    <span class="text-gray-300 font-medium capitalize">
                      {key === 'bonusValue' ? 'Bonus Value' : key}
                    </span>
                    <span class="text-white font-bold">{value}/10</span>
                  </div>
                  <div class="w-full bg-gray-700 rounded-full h-2">
                    <div 
                      class="bg-gradient-to-r from-amber-500 to-orange-500 h-2 rounded-full"
                      style={`width: ${(value / 10) * 100}%`}
                    ></div>
                  </div>
                </div>
              ))}
            </div>
          )}
        </div>
      </section>

      <!-- Call to Action -->
      <div class="review-cta-section relative overflow-hidden">
        <div class="absolute inset-0 bg-gradient-to-r from-gray-800/20 to-gray-700/20 backdrop-blur-sm"></div>
        <div class="absolute top-0 left-0 w-40 h-40 bg-gray-700/10 rounded-full -ml-20 -mt-20"></div>
        <div class="absolute bottom-0 right-0 w-32 h-32 bg-gray-700/10 rounded-full -mr-16 -mb-16"></div>
        <div class="relative">
          <div class="text-center mb-8">
            <div class="w-16 h-16 bg-gray-700/20 rounded-2xl flex items-center justify-center text-3xl mx-auto mb-4">
              🎆
            </div>
            <h3 class="text-4xl font-bold mb-4">Ready to Play at {casino.brand}?</h3>
            <p class="text-white/90 mb-8 text-xl max-w-2xl mx-auto leading-relaxed">
              Join thousands of players enjoying great games and bonuses!
            </p>
          </div>
          
          <div class="flex flex-col sm:flex-row justify-center items-center gap-6 mb-8">
            <a 
              href={casino.affiliate?.link || casino.url || '#'} 
              target="_blank" 
              rel="noopener noreferrer" 
              class="inline-flex items-center bg-white text-casino-700 px-10 py-5 rounded-2xl font-bold text-lg hover:bg-gray-100 transition-all duration-300 transform hover:scale-105 shadow-2xl hover:shadow-3xl"
            >
              <svg class="w-6 h-6 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"></path>
              </svg>
              Visit {casino.brand} →
              <svg class="w-5 h-5 ml-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
              </svg>
            </a>
            
            <a 
              href="/reviews" 
              class="inline-flex items-center border-2 border-white/80 text-white px-10 py-5 rounded-2xl font-bold text-lg hover:bg-white hover:text-casino-700 transition-all duration-300 transform hover:scale-105 backdrop-blur-sm"
            >
              <svg class="w-6 h-6 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
              </svg>
              Compare More Casinos
            </a>
          </div>
          
          <div class="text-center">
            <div class="inline-flex items-center space-x-6 bg-gray-800/10 backdrop-blur-sm rounded-2xl px-8 py-4 border border-gray-700/20">
              <div class="flex items-center space-x-2">
                <svg class="w-5 h-5 text-green-400" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M2.166 4.999A11.954 11.954 0 0010 1.944 11.954 11.954 0 0017.834 5c.11.65.166 1.32.166 2.001 0 5.225-3.34 9.67-8 11.317C5.34 16.67 2 12.225 2 7c0-.682.057-1.35.166-2.001zm11.541 3.708a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                </svg>
                <span class="text-white/90 text-sm font-medium">Licensed & Regulated</span>
              </div>
              <div class="flex items-center space-x-2">
                <svg class="w-5 h-5 text-blue-400" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z" clip-rule="evenodd"></path>
                </svg>
                <span class="text-white/90 text-sm font-medium">Secure Gaming</span>
              </div>
              <div class="flex items-center space-x-2">
                <svg class="w-5 h-5 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path>
                </svg>
                <span class="text-white/90 text-sm font-medium">Fair Play Certified</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </article>
  </main>
</SimplePageLayout>