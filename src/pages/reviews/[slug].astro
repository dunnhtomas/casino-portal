---
import SimplePageLayout from '../../components/Layout/SimplePageLayout.astro';
import { NavigationCoordinator } from '../../coordinators/NavigationCoordinator';

// Define Casino interface matching actual data structure
interface CasinoData {
  slug: string;
  brand: string;
  url?: string;
  licences?: string[];
  restrictedGeos?: string[];
  ratings?: {
    security?: number;
    fairness?: number;
    payout?: number;
    bonusValue?: number;
    games?: number;
    support?: number;
    reputation?: number;
    [key: string]: number | undefined;
  };
  bonuses?: {
    welcome?: {
      headline?: string;
      wagering?: string;
      maxCashout?: number;
    };
  };
  providers?: string[];
  banking?: string[];
  topTags?: string[];
  logo?: {
    url?: string;
  };
  safety?: {
    yearsOnline?: number;
  };
  payoutSpeedHours?: number;
  affiliate?: {
    link?: string;
  };
}

// Generate static paths for all casino reviews
export async function getStaticPaths() {
  // Use import.meta.glob to load JSON at build time
  const dataFiles = import.meta.glob('../../../data/*.json', { eager: true });
  const casinosFile = dataFiles['../../../data/casinos.json'] as any;
  const casinosData = casinosFile?.default || [];
  
  return casinosData.map((casino: any) => ({
    params: { slug: casinoData.slug },
    props: { casino }
  }));
}

interface Props {
  casino: CasinoData;
}

const { casino } = Astro.props;
const { slug } = Astro.params;

// Type assertion to ensure TypeScript recognizes casino properties
const casinoData = casino as CasinoData;

// Initialize navigation coordinator
const navigationCoordinator = new NavigationCoordinator();

// Generate page metadata
const pageTitle = `${casinoData.brand} Review - Expert Analysis & Ratings`;
const pageDescription = `Comprehensive ${casinoData.brand} review with detailed analysis of games, bonuses, security, and customer service`;

// Generate rating stars
function generateStars(rating: number) {
  return Array(5).fill(0).map((_, i) => i < Math.floor(rating / 2) ? '★' : '☆').join('');
}

// Calculate overall rating with proper typing
const overallRating = casinoData.ratings ? 
  Object.values(casinoData.ratings as any).reduce((sum: number, val: any) => sum + (Number(val) || 0), 0) / Object.keys(casinoData.ratings).length : 9.0;

// Generate breadcrumbs using navigation coordinator
const breadcrumbs = navigationCoordinator.generateBreadcrumbs(`/reviews/${casinoData.slug}`);
---

<SimplePageLayout 
  title={pageTitle}
  description={pageDescription}
  breadcrumbs={breadcrumbs}
>
  <main class="min-h-screen bg-gradient-to-br from-cream-50 to-casino-50">
    <article class="container mx-auto px-6 py-8 max-w-7xl">
      
      <!-- Hero Section with your custom classes -->
      <div class="review-hero">
        <div class="review-brand-info">
          <div class="flex items-start space-x-6">
            <div class="flex-shrink-0">
              <img 
                src={casinoData.logo?.url || `/images/casinos/${casinoData.slug}.png` || '/images/casinos/default-casinoData.svg'} 
                alt={`${casinoData.brand} logo`} 
                class="w-20 h-20 object-contain rounded-2xl shadow-lg bg-white p-2 border border-gray-200"
                loading="lazy"
              />
            </div>
            <div class="flex-grow">
              <h1 class="review-brand-title">{casinoData.brand}</h1>
              <p class="review-brand-subtitle mb-4">Comprehensive Expert Review & Analysis</p>
              <div class="flex flex-wrap gap-2 mb-4">
                {casinoData.topTags?.map((tag: string) => (
                  <span class="bg-casino-100 text-casino-800 px-3 py-1 rounded-full text-sm font-medium">
                    {tag}
                  </span>
                )) || (
                  <>
                    <span class="bg-casino-100 text-casino-800 px-3 py-1 rounded-full text-sm font-medium">
                      Fast payouts
                    </span>
                    <span class="bg-casino-100 text-casino-800 px-3 py-1 rounded-full text-sm font-medium">
                      Premium license
                    </span>
                    <span class="bg-casino-100 text-casino-800 px-3 py-1 rounded-full text-sm font-medium">
                      Great game variety
                    </span>
                  </>
                )}
              </div>
            </div>
          </div>
          <div class="review-rating-section">
            <div class="bg-gradient-to-r from-amber-500 to-orange-500 text-white px-8 py-4 rounded-2xl font-bold text-3xl inline-block shadow-lg">
              <div class="flex items-center space-x-2">
                <svg class="w-8 h-8" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path>
                </svg>
                <span>{overallRating ? overallRating.toFixed(1) : '9.0'}</span>
              </div>
            </div>
            <p class="text-sm text-gray-600 mt-2 font-medium">
              {overallRating >= 9 ? 'Excellent' : overallRating >= 8 ? 'Very Good' : overallRating >= 7 ? 'Good' : 'Fair'} Rating
            </p>
            <p class="text-xs text-gray-500 mt-1">Overall Rating</p>
          </div>
        </div>
      </div>

      <!-- Quick Facts Grid using your custom classes -->
      <section class="review-content-section">
        <h2 class="review-section-title">Quick Facts & Overview</h2>
        <div class="review-quick-facts">
          
          <!-- Safety & Security Card -->
          <div class="review-quick-fact-item">
            <div class="w-12 h-12 bg-gradient-to-r from-casino-500 to-casino-600 rounded-xl flex items-center justify-center text-white text-xl mb-4 mx-auto shadow-lg">
              🛡️
            </div>
            <h3 class="font-bold text-gray-900 mb-4 text-lg">Safety & Security</h3>
            <div class="space-y-3">
              <div class="flex justify-between items-center">
                <span class="text-sm text-gray-600 font-medium">License</span>
                <span class="text-sm font-bold text-gray-900 bg-gray-100 px-2 py-1 rounded-md">
                  {casinoData.licences?.join(', ') || 'Malta'}
                </span>
              </div>
              <div class="flex justify-between items-center">
                <span class="text-sm text-gray-600 font-medium">Years Online</span>
                <span class="text-sm font-bold text-gray-900 bg-gray-100 px-2 py-1 rounded-md">
                  {casinoData.safety?.yearsOnline || '4'}
                </span>
              </div>
              <div class="flex justify-between items-center">
                <span class="text-sm text-gray-600 font-medium">Security Score</span>
                <span class="text-sm font-bold text-gray-900 bg-gray-100 px-2 py-1 rounded-md">
                  {casinoData.ratings?.security ? `${casinoData.ratings.security}/10` : '10/10'}
                </span>
              </div>
            </div>
          </div>

          <!-- Banking & Payments Card -->
          <div class="review-quick-fact-item">
            <div class="w-12 h-12 bg-gradient-to-r from-green-500 to-green-600 rounded-xl flex items-center justify-center text-white text-xl mb-4 mx-auto shadow-lg">
              💳
            </div>
            <h3 class="font-bold text-gray-900 mb-4 text-lg">Banking & Payments</h3>
            <div class="space-y-3">
              <div class="flex justify-between items-center">
                <span class="text-sm text-gray-600 font-medium">Payment Methods</span>
                <span class="text-sm font-bold text-gray-900 bg-gray-100 px-2 py-1 rounded-md">
                  {casinoData.banking?.length || '6'}
                </span>
              </div>
              <div class="flex justify-between items-center">
                <span class="text-sm text-gray-600 font-medium">Payout Speed</span>
                <span class="text-sm font-bold text-gray-900 bg-gray-100 px-2 py-1 rounded-md">
                  {casinoData.payoutSpeedHours ? `${casinoData.payoutSpeedHours}h` : '24h'}
                </span>
              </div>
              <div class="flex justify-between items-center">
                <span class="text-sm text-gray-600 font-medium">Payout Rating</span>
                <span class="text-sm font-bold text-gray-900 bg-gray-100 px-2 py-1 rounded-md">
                  {casinoData.ratings?.payout ? `${casinoData.ratings.payout}/10` : '9.5/10'}
                </span>
              </div>
            </div>
          </div>

          <!-- Games & Software Card -->
          <div class="review-quick-fact-item">
            <div class="w-12 h-12 bg-gradient-to-r from-purple-500 to-purple-600 rounded-xl flex items-center justify-center text-white text-xl mb-4 mx-auto shadow-lg">
              🎲
            </div>
            <h3 class="font-bold text-gray-900 mb-4 text-lg">Games & Software</h3>
            <div class="space-y-3">
              <div class="flex justify-between items-center">
                <span class="text-sm text-gray-600 font-medium">Game Providers</span>
                <span class="text-sm font-bold text-gray-900 bg-gray-100 px-2 py-1 rounded-md">
                  {casinoData.providers?.length || '6'}
                </span>
              </div>
              <div class="flex justify-between items-center">
                <span class="text-sm text-gray-600 font-medium">Games Rating</span>
                <span class="text-sm font-bold text-gray-900 bg-gray-100 px-2 py-1 rounded-md">
                  {casinoData.ratings?.games ? `${casinoData.ratings.games}/10` : '10/10'}
                </span>
              </div>
              <div class="flex justify-between items-center">
                <span class="text-sm text-gray-600 font-medium">Live Dealer</span>
                <span class="text-sm font-bold text-gray-900 bg-gray-100 px-2 py-1 rounded-md">
                  {casinoData.providers?.some((p: string) => p.toLowerCase().includes('evolution')) ? 'Yes' : 'Available'}
                </span>
              </div>
            </div>
          </div>

          <!-- Bonuses & Promotions Card -->
          <div class="review-quick-fact-item">
            <div class="w-12 h-12 bg-gradient-to-r from-amber-500 to-amber-600 rounded-xl flex items-center justify-center text-white text-xl mb-4 mx-auto shadow-lg">
              🎁
            </div>
            <h3 class="font-bold text-gray-900 mb-4 text-lg">Bonuses & Promotions</h3>
            <div class="space-y-3">
              <div class="flex justify-between items-center">
                <span class="text-sm text-gray-600 font-medium">Welcome Bonus</span>
                <span class="text-sm font-bold text-gray-900 bg-gray-100 px-2 py-1 rounded-md">
                  Available
                </span>
              </div>
              <div class="flex justify-between items-center">
                <span class="text-sm text-gray-600 font-medium">Wagering</span>
                <span class="text-sm font-bold text-gray-900 bg-gray-100 px-2 py-1 rounded-md">
                  {casinoData.bonuses?.welcome?.wagering || '35x'}
                </span>
              </div>
              <div class="flex justify-between items-center">
                <span class="text-sm text-gray-600 font-medium">Bonus Rating</span>
                <span class="text-sm font-bold text-gray-900 bg-gray-100 px-2 py-1 rounded-md">
                  {casinoData.ratings?.bonusValue ? `${casinoData.ratings.bonusValue}/10` : '9/10'}
                </span>
              </div>
            </div>
          </div>

        </div>
      </section>

      <!-- Detailed Review Content -->
      <section class="review-content-section">
        <h2 class="review-section-title">Detailed Review Analysis</h2>
        <div class="prose max-w-none">
          <p class="text-gray-700 leading-relaxed text-lg mb-6">
            Our expert review team has thoroughly analyzed {casinoData.brand} to provide you with comprehensive insights about this online casinoData. 
            Licensed under {casinoData.licences?.join(' and ') || 'Malta'}, this casino offers a secure gaming environment 
            with {casinoData.providers?.length || 'multiple'} game providers and 
            {casinoData.banking?.length || 'various'} payment methods.
          </p>
          
          {casinoData.bonuses?.welcome && (
            <div class="mt-6 p-6 bg-gradient-to-r from-amber-50 to-orange-50 border border-amber-200 rounded-xl">
              <h3 class="text-xl font-bold text-amber-800 mb-3 flex items-center">
                <svg class="w-6 h-6 mr-2" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path>
                </svg>
                Welcome Bonus
              </h3>
              <p class="text-amber-800 font-semibold">
                New players can enjoy {casinoData.bonuses.welcome.headline || '€600 + 120 FS'} with {casinoData.bonuses.welcome.wagering || '35x'} wagering requirements.
              </p>
            </div>
          )}
          
          {casinoData.providers && casinoData.providers.length > 0 && (
            <div class="mt-8">
              <h3 class="text-xl font-bold text-gray-900 mb-4">Game Providers</h3>
              <div class="flex flex-wrap gap-3">
                {casinoData.providers.slice(0, 8).map((provider: string) => (
                  <span class="bg-gradient-to-r from-casino-100 to-casino-200 text-casino-800 px-4 py-2 rounded-lg text-sm font-medium border border-casino-300">
                    {provider}
                  </span>
                ))}
                {casinoData.providers.length > 8 && (
                  <span class="bg-gray-100 text-gray-600 px-4 py-2 rounded-lg text-sm font-medium border border-gray-300">
                    +{casinoData.providers.length - 8} more
                  </span>
                )}
              </div>
            </div>
          )}
        </div>
      </section>

      <!-- Rating Breakdown using your custom classes -->
      <section class="review-content-section">
        <h2 class="review-section-title">Rating Breakdown</h2>
        <div class="flex justify-between items-center mb-6">
          <p class="text-gray-600">Our comprehensive rating methodology evaluates multiple factors</p>
          <a href="/guides/how-we-rate" class="text-casino-600 hover:text-casino-700 text-sm font-medium">
            How We Rate →
          </a>
        </div>
        
        {casinoData.ratings && (
          <div class="review-rating-grid">
            {Object.entries(casinoData.ratings).map(([key, value]: [string, any]) => (
              <div class="review-rating-item">
                <h4 class="font-semibold text-gray-900 mb-2 capitalize">
                  {key === 'bonusValue' ? 'Bonus Value' : key}
                </h4>
                <div class="text-3xl font-bold text-casino-600 mb-2">{value}/10</div>
                <div class="w-full bg-gray-200 rounded-full h-3">
                  <div 
                    class="bg-gradient-to-r from-casino-500 to-casino-600 h-3 rounded-full transition-all duration-300"
                    style={`width: ${(value / 10) * 100}%`}
                  ></div>
                </div>
                <div class="text-sm text-gray-600 mt-2">
                  {value >= 9 ? 'Excellent' : value >= 8 ? 'Very Good' : value >= 7 ? 'Good' : 'Fair'}
                </div>
              </div>
            ))}
          </div>
        )}
      </section>

      <!-- Call to Action using your custom classes -->
      <div class="review-cta-section">
        <div class="text-center mb-8">
          <div class="w-16 h-16 bg-white/20 rounded-2xl flex items-center justify-center text-3xl mx-auto mb-4">
            🎆
          </div>
          <h3 class="text-4xl font-bold mb-4">Ready to Play at {casinoData.brand}?</h3>
          <p class="text-white/90 mb-8 text-xl max-w-2xl mx-auto leading-relaxed">
            Join thousands of players enjoying great games and bonuses!
          </p>
        </div>
        
        <div class="flex flex-col sm:flex-row justify-center items-center gap-6 mb-8">
          <a 
            href={casinoData.affiliate?.link || casinoData.url || '#'} 
            target="_blank" 
            rel="noopener noreferrer" 
            class="inline-flex items-center bg-white text-casino-700 px-10 py-5 rounded-2xl font-bold text-lg hover:bg-gray-100 transition-all duration-300 transform hover:scale-105 shadow-2xl hover:shadow-3xl"
          >
            <svg class="w-6 h-6 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"></path>
            </svg>
            Visit {casinoData.brand} →
            <svg class="w-5 h-5 ml-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
            </svg>
          </a>
          
          <a 
            href="/reviews" 
            class="inline-flex items-center border-2 border-white/80 text-white px-10 py-5 rounded-2xl font-bold text-lg hover:bg-white hover:text-casino-700 transition-all duration-300 transform hover:scale-105 backdrop-blur-sm"
          >
            <svg class="w-6 h-6 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
            </svg>
            Compare More Casinos
          </a>
        </div>
        
        <div class="text-center">
          <div class="inline-flex items-center space-x-6 bg-white/10 backdrop-blur-sm rounded-2xl px-8 py-4 border border-white/20">
            <div class="flex items-center space-x-2">
              <svg class="w-5 h-5 text-green-400" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M2.166 4.999A11.954 11.954 0 0010 1.944 11.954 11.954 0 0017.834 5c.11.65.166 1.32.166 2.001 0 5.225-3.34 9.67-8 11.317C5.34 16.67 2 12.225 2 7c0-.682.057-1.35.166-2.001zm11.541 3.708a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
              </svg>
              <span class="text-white/90 text-sm font-medium">Licensed & Regulated</span>
            </div>
            <div class="flex items-center space-x-2">
              <svg class="w-5 h-5 text-blue-400" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z" clip-rule="evenodd"></path>
              </svg>
              <span class="text-white/90 text-sm font-medium">Secure Gaming</span>
            </div>
            <div class="flex items-center space-x-2">
              <svg class="w-5 h-5 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
                <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path>
              </svg>
              <span class="text-white/90 text-sm font-medium">Fair Play Certified</span>
            </div>
          </div>
        </div>
      </div>
    </article>
  </main>
</SimplePageLayout>